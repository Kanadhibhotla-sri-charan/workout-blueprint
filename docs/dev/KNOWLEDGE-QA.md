# Knowledge QA Report

**Generated by:** `npm run data-report` (`scripts/data-report.js`) — regenerate after any dataset change rather than editing this file by hand.
**Generated at:** 2026-08-16T08:17:30.071Z

This report answers "what does the dataset currently look like?" `npm run validate-data` answers "is it valid?" — keep the two separate per Task K of the [Phase 2 spec](../architecture/PHASE-2-SCHEMA-AND-DATA-GOVERNANCE.md).

## Totals

- Total canonical exercises: **123**
- Reviewed: **123**
- Needs-review: **0**
- Draft: **0**

## Validation summary

- Schema violations: **0**
- Taxonomy violations: **0**
- Invalid relationships: **0**
- Review-governance violations: **0**
- Duplicate IDs: **0**
- Duplicate canonical exercises (same name, different ids): **0**
- File-level load errors: **0**

Full violation detail: run `npm run validate-data`. This report only summarizes counts.

## Cross-module exercises

**1** record(s) declare more than one `body_regions` value (the ADR 0001 dedup pattern):

- `romanian-deadlift` (data/exercises/hamstrings.yaml) — hips, hamstrings

## Content completeness (optional fields)

Per [ADR 0002](../adr/0002-empty-field-semantics.md), an optional field left as a true empty list means "not yet established," not "not applicable" (that's what `null` means). Counts below are records where the field is still empty — this is the honest, current content backlog, not a defect.

| Field | Empty (not yet established) | Marked `null` (not applicable) |
|---|---|---|
| `secondary_targets` | 31/123 | 0/123 |
| `advantages` | 123/123 | 0/123 |
| `technique_cues` | 123/123 | 0/123 |
| `common_mistakes` | 123/123 | 0/123 |
| `programming_notes` | 119/123 | 0/123 |
| `alternatives` | 123/123 | 0/123 |
| `overlaps_with` | 12/123 | 0/123 |
| `evidence_notes` | 77/123 | 0/123 |

## Evidence and mirror-effect coverage

- Exercises missing `mirror_effect`: **0**
- Exercises with claim-like language but no `evidence_notes` (heuristic, verify by hand): **4** — back-extension-45-spinal-dominant, back-extension-45-hip-dominant, hack-squat, bulgarian-split-squat-knee-dominant

## Known, logged exceptions (not bugs — see the linked reasoning)

- **`advantages` is empty on all 123 records.** Resolved by architect decision, not a gap: kept as a retirement candidate, deliberately not bulk-populated, its emptiness never blocks `reviewed` — see [`docs/architecture/PHASE-2-OPEN-DECISIONS.md`](../architecture/PHASE-2-OPEN-DECISIONS.md) and [`docs/dev/reports/REVIEW-PROMOTION-GATE.md`](reports/REVIEW-PROMOTION-GATE.md).
- **`alternatives` is empty on all 123 records.** Resolved by architect decision: the field is kept (not retired) and precisely defined against `complements`/`overlaps_with`, to be populated selectively when a genuine substitution exists rather than bulk-filled — see [`docs/architecture/PHASE-2-OPEN-DECISIONS.md`](../architecture/PHASE-2-OPEN-DECISIONS.md) and [`SCHEMA.md`](../knowledge-manual/SCHEMA.md#the-three-relationship-fields-defined-precisely).

